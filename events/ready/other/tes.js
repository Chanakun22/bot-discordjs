// const mongoose = require('mongoose');
// const TestModel = require('../../Schemas/tiktok_noti_db'); // แทนที่ด้วย path ที่ถูกต้อง แทนที่ด้วย path ที่ถูกต้องของ model ของคุณ

// // ตัวอย่างการเชื่อมต่อกับ MongoDB
// // ตัวอย่างการค้นหาข้อมูลทั้งหมดในคอลเลกชันด้วย async/await
// async function findAllDocuments() {
//     try {
//         // const docs = await TestModel.find({});
//         // const docs = await TestModel.deleteMany({});
//         console.log(docs); // แสดงข้อมูลที่ค้นหาได้
//     } catch (err) {
//         console.error(err);
//     }
// }

// findAllDocuments();

